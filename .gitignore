node_modules/
dist/
.wrangler/
*.tsbuildinfo

# Local secrets — never commit
worker/.dev.vars
upstream/.dev.vars

# Production config — contains real values, not for source control
worker/wrangler.production.toml

.idea

*.log
